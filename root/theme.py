# Root CLI color palette
# Primary accent: #dffb0a (electric yellow-green)
# Supporting: shades of grey + warm yellow-green tints

ACCENT       = "#dffb0a"   # primary — electric yellow-green
ACCENT_DIM   = "#a8bc07"   # muted accent for secondary highlights
ACCENT_SOFT  = "#eafd6e"   # lighter tint for file names, success details

SUCCESS      = "#dffb0a"   # same as accent — "it worked"
WARNING      = "#c9d400"   # slightly more yellow, warnings
ERROR        = "#ff5f5f"   # red kept for genuine errors only
REFUSE       = "#ff5f5f"

FG           = "#e8e8e8"   # near-white default text
FG_DIM       = "#666666"   # subdued labels, hints
FG_MUTED     = "#999999"   # even lighter — paths, metadata
BORDER       = "#333333"   # rule lines, dividers

SHELL_LABEL  = "#888888"   # shell indicator in prompt
CWD          = ACCENT      # current directory in prompt — draws the eye
